import AdminAgenda from "@/pages/admin/AdminAgenda";

// Agenda do corretor logado (mesma tela da agenda geral, restrita a "minha agenda").
const AgendaPage = () => <AdminAgenda mineOnly />;

export default AgendaPage;
